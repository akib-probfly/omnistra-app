import { useState } from 'react';
import { KeyboardAvoidingView, Pressable, StyleSheet, Text, TextInput, View } from 'react-native';
import { useAuth } from '../auth/AuthContext';

export function LoginScreen({ onRegister }: { onRegister: () => void }) { const { login } = useAuth(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); async function submit() { setBusy(true); setError(''); try { await login(email, password); } catch (e) { setError(e instanceof Error ? e.message : 'Unable to sign in.'); } finally { setBusy(false); } } return <KeyboardAvoidingView style={styles.screen} behavior="padding"><View style={styles.brand}><Text style={styles.logo}>omnistra</Text><Text style={styles.tagline}>Your omnichannel inbox, everywhere.</Text></View><View style={styles.card}><Text style={styles.title}>Welcome back</Text><Text style={styles.subtitle}>Sign in to your workspace</Text><TextInput autoCapitalize="none" keyboardType="email-address" placeholder="Email address" placeholderTextColor="#94a3b8" style={styles.input} value={email} onChangeText={setEmail} /><TextInput placeholder="Password" placeholderTextColor="#94a3b8" secureTextEntry style={styles.input} value={password} onChangeText={setPassword} />{error ? <Text style={styles.error}>{error}</Text> : null}<Pressable disabled={busy} onPress={submit} style={styles.primary}><Text style={styles.primaryText}>{busy ? 'Signing in...' : 'Sign in'}</Text></Pressable><Pressable onPress={onRegister}><Text style={styles.link}>Create a new account</Text></Pressable></View></KeyboardAvoidingView>; }
const styles = StyleSheet.create({ screen: { backgroundColor: '#f4f8ff', flex: 1, justifyContent: 'center', padding: 20 }, brand: { marginBottom: 24, paddingHorizontal: 8 }, logo: { color: '#2563eb', fontSize: 30, fontWeight: '800' }, tagline: { color: '#64748b', fontSize: 14, marginTop: 5 }, card: { backgroundColor: '#fff', borderColor: '#dbe7fa', borderRadius: 22, borderWidth: 1, padding: 22 }, title: { color: '#0f172a', fontSize: 24, fontWeight: '800' }, subtitle: { color: '#64748b', marginTop: 6 }, input: { borderColor: '#dbe4f1', borderRadius: 12, borderWidth: 1, color: '#0f172a', height: 50, marginTop: 16, paddingHorizontal: 14 }, primary: { alignItems: 'center', backgroundColor: '#2563eb', borderRadius: 12, marginTop: 18, padding: 15 }, primaryText: { color: '#fff', fontWeight: '700' }, link: { color: '#2563eb', fontWeight: '600', marginTop: 20, textAlign: 'center' }, error: { color: '#dc2626', fontSize: 13, marginTop: 12 } });

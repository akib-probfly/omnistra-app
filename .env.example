EXPO_PUBLIC_API_BASE_URL=https://api.omnistra.ai/api/v1
# Optional. Leave empty to use the API origin. Empty string is ignored (falls back to API origin).
EXPO_PUBLIC_SOCKET_URL=
# Public web app origin. Used for PipraPay return URLs (must be https).
EXPO_PUBLIC_FRONTEND_BASE_URL=https://app.omnistra.ai
